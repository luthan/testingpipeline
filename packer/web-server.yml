---
- hosts: all
  become: yes
  become_method: sudo
  tasks:
  - name: ensure latest apache is installed
    apt:
      name: httpd
      state: latest
  - name: ensure apache is running
    service:
      name: httpd
      state: started